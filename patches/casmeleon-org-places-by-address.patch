diff --git a/pkg/asm/Directives.go b/pkg/asm/Directives.go
index addb2be..10d8c5b 100644
--- a/pkg/asm/Directives.go
+++ b/pkg/asm/Directives.go
@@ -12,12 +12,11 @@ type DirectiveOrg struct {
 	address uint32
 }
 
+//Assemble moves the address and emits nothing. Unlike .advance it may go
+//backwards: it places what follows at an address instead of padding up to one,
+//so a source can lay out its regions in any order it likes.
 func (d *DirectiveOrg) Assemble(m opcodes.VM, addr uint32, index int, ctx Context) (uint32, []uint8, error) {
-	target := d.address * (ctx.ByteSize() / 8)
-	if addr > target {
-		return 0, emptyLabelOutput, errors.New(".org directive cannot change PC backwards")
-	}
-	return target, emptyLabelOutput, nil
+	return d.address * (ctx.ByteSize() / 8), emptyLabelOutput, nil
 }
 
 func (d *DirectiveOrg) IsAddressInvariant() bool {
diff --git a/pkg/asm/Run.go b/pkg/asm/Run.go
index eff27c8..fe79419 100644
--- a/pkg/asm/Run.go
+++ b/pkg/asm/Run.go
@@ -21,7 +21,13 @@ const maxPasses = 10
 //that is when it depends on a symbol that moved during the previous pass, or
 //when it depends on its own address and that address changed. Everything else
 //keeps the bytes of the previous pass, because they cannot have changed: only
-//the position in the output moves, and that follows from the concatenation.
+//the position in the output moves, and that follows from the address each item
+//was assembled at.
+//
+//The next address is remembered rather than recomputed from the byte count. The
+//two are equal for everything that emits what it spans, but not for .org, which
+//moves the address and emits nothing: recomputing made a skipped .org into a
+//silent no-op.
 //
 //What must not happen is reassembling a subset of the items at the addresses
 //recorded during an earlier pass. Label.Assemble writes the address it receives
@@ -31,6 +37,7 @@ const maxPasses = 10
 func AssembleSource(m opcodes.VM, list []Compilable, ctx Context) ([]uint8, error) {
 	result := make([]BinaryImage, len(list))
 	lastAddr := make([]uint32, len(list))
+	nextAddr := make([]uint32, len(list))
 	known := make([]bool, len(list))
 
 	for pass := 0; pass < maxPasses; pass++ {
@@ -53,8 +60,8 @@ func AssembleSource(m opcodes.VM, list []Compilable, ctx Context) ([]uint8, erro
 			//the bytes cannot have changed when no symbol this item depends on
 			//has moved and either its address is irrelevant or it did not move
 			if known[j] && !dirty[j] && (here == lastAddr[j] || item.IsAddressInvariant()) {
+				addr = nextAddr[j] + (here - lastAddr[j])
 				lastAddr[j] = here
-				addr = here + uint32(len(result[j].content))
 				continue
 			}
 
@@ -64,6 +71,7 @@ func AssembleSource(m opcodes.VM, list []Compilable, ctx Context) ([]uint8, erro
 			}
 			result[j] = BinaryImage{img}
 			lastAddr[j] = here
+			nextAddr[j] = next
 			known[j] = true
 			addr = next
 			work++
@@ -79,9 +87,18 @@ func AssembleSource(m opcodes.VM, list []Compilable, ctx Context) ([]uint8, erro
 		}
 		fmt.Println("Addresses stable, done in", pass+1, "pass(es)")
 
-		img := []uint8{}
-		for _, bin := range result {
-			img = append(img, bin.content...)
+		//Placed by address, not by concatenation: an item's bytes belong at the
+		//address it was assembled at, which is the only reading under which
+		//.org means anything and under which two of them may run backwards.
+		size := uint32(0)
+		for j := range result {
+			if end := lastAddr[j] + uint32(len(result[j].content)); end > size {
+				size = end
+			}
+		}
+		img := make([]uint8, size)
+		for j, bin := range result {
+			copy(img[lastAddr[j]:], bin.content)
 		}
 		return img, nil
 	}
