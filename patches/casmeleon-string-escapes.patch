diff --git a/pkg/scanner/Scanner.go b/pkg/scanner/Scanner.go
index 574dcd2..cb69071 100644
--- a/pkg/scanner/Scanner.go
+++ b/pkg/scanner/Scanner.go
@@ -93,6 +93,17 @@ func ClassifyMergeableTokens(tokens []Token) {
 	}
 }
 
+//endsWithOddBackslashes reports whether the token ends in a run of an odd
+//number of backslashes, which means the delimiter that follows is escaped
+//rather than closing the literal
+func endsWithOddBackslashes(t Token) bool {
+	n := 0
+	for i := len(t.slice) - 1; i >= 0 && t.slice[i] == '\\'; i-- {
+		n++
+	}
+	return n%2 == 1
+}
+
 //MergeASMLine matching tokens
 func MergeASMLine(line []Token) []Token {
 	valid := []Token{}
@@ -111,8 +122,9 @@ func MergeASMLine(line []Token) []Token {
 			valid = append(valid, t)
 		} else {
 			if match != -1 {
+				escaped := endsWithOddBackslashes(*merged)
 				*merged = merged.Merge(t)
-				if match == t.basicID {
+				if match == t.basicID && !escaped {
 					valid = append(valid, *merged)
 					match = -1
 					merged = nil
diff --git a/cmd/casmeleon/AssemblyParser.go b/cmd/casmeleon/AssemblyParser.go
index 34a088a..45f2bc2 100644
--- a/cmd/casmeleon/AssemblyParser.go
+++ b/cmd/casmeleon/AssemblyParser.go
@@ -11,8 +11,36 @@ import (
 	"github.com/aleferri/casmeleon/pkg/text"
 )
 
+//UnescapeString resolves the backslash escapes the scanner preserved inside a
+//quoted literal.  An unknown escape yields the character itself, so that a
+//lone backslash is never silently dropped.
+func UnescapeString(s string) string {
+	var b strings.Builder
+	runes := bytes.Runes([]byte(s))
+	for i := 0; i < len(runes); i++ {
+		if runes[i] != '\\' || i+1 >= len(runes) {
+			b.WriteRune(runes[i])
+			continue
+		}
+		i++
+		switch runes[i] {
+		case 'n':
+			b.WriteRune('\n')
+		case 'r':
+			b.WriteRune('\r')
+		case 't':
+			b.WriteRune('\t')
+		case '0':
+			b.WriteRune(rune(0))
+		default:
+			b.WriteRune(runes[i])
+		}
+	}
+	return b.String()
+}
+
 func IsDirective(s string) bool {
-	return s == ".advance" || s == ".org" || s == ".alias" || s == ".db" || s == ".dw"
+	return s == ".advance" || s == ".org" || s == ".alias" || s == ".db" || s == ".dw" || s == ".dd"
 }
 
 //ParseDepositValues consumes the comma separated value list of a .db or a .dw.
@@ -40,7 +68,7 @@ func ParseDepositValues(lang casm.Language, stream parser.Stream, table *SymbolT
 		switch tok.ID() {
 		case text.QuotedString:
 			{
-				str := strings.TrimSuffix(strings.TrimPrefix(tok.Value(), "\""), "\"")
+				str := UnescapeString(strings.TrimSuffix(strings.TrimPrefix(tok.Value(), "\""), "\""))
 				for _, c := range bytes.Runes([]byte(str)) {
 					values = append(values, asm.MakeConstant(int64(c)))
 				}
